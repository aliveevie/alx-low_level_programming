#include "holberton.h"
/**
 * add - a function that adds two intergers and returns the result
 * #one: firdt number input
 * @two: second number input
 * Return: one + two
 */
int add(int one, int two)
{
	
	return (one + two);

}
